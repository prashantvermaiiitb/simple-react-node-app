/**
 * Here we are going to Render the component and then fetch the data.
 * useEffect() ==> calling ==> fetch() Api
 *
 * * We call this approach “fetch-on-render” because it doesn’t start fetching until after the
 * * component has rendered on the screen. This leads to a problem known as a “waterfall”.
 */
